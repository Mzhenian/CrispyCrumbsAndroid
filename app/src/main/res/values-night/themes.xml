<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CrispyCrumbs" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>


    <!-- DarkMode -->
    <!-- Screen background -->
    <style name="SCREEN">
        <item name="android:background">@color/black_div</item>
    </style>

    <!-- DarkMode -->
    <style name="TEXT">
        <item name="android:textAppearance">@style/TextAppearance.BoldText</item>
        <item name="android:textColor">@color/off_white</item>
    </style>

    <!-- DarkMode -->
    <style name="BUTTON">
        <item name="android:background">@drawable/small_button_style_light</item>
        <item name="android:textColor">@android:color/holo_orange_light</item>
        <item name="android:padding">8dp</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="SEARCHBAR">
        <item name="android:background">@color/black_div</item>
    </style>
</resources>